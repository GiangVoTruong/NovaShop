# Local dev: copy thành .env — Vite proxy /api → Render (vite.config.ts)
VITE_API_BASE_URL=/api

# Production build: dùng .env.production (đã commit) hoặc set trên Render:
# VITE_API_BASE_URL=https://novashop-e4ir.onrender.com/api
