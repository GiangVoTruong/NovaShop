# Copy to .env và chỉnh nếu backend không chạy localhost:8080
# Dev (pnpm dev): dùng /api — Vite proxy sang http://localhost:8080
VITE_API_BASE_URL=/api

# Production: URL đầy đủ tới backend
# VITE_API_BASE_URL=https://api.example.com
